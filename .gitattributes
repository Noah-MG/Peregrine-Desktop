# Normalise line endings so the repo stays consistent across machines.
* text=auto eol=lf

# Windows batch files must keep CRLF or cmd.exe can mis-parse them.
*.bat text eol=crlf
*.cmd text eol=crlf

# Value tables and any other binary payload must never be converted.
*.BIN binary
*.bin binary
